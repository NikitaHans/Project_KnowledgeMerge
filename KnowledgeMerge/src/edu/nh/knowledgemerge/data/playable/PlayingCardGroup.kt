package edu.nh.knowledgemerge.data.playable

import java.util.*

class PlayingCardGroup {

    private val id: UUID = UUID.randomUUID()

    init {
        println("Initialized Playing Card Group with id %s".format(id))
    }
}